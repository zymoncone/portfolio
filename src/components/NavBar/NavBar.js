import "./NavBar.css"

const NavBar = () => {

  return (
    <nav className="nav-bar">
      <div className="nav-item">
        <button className="button-fancy">Resume</button>
      </div>
    </nav>
  )
}

export default NavBar